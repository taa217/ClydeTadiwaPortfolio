{
  "version": 2,
  "builds": [
    { "src": "client/index.html", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/client/(.*)", "dest": "/client/$1" },
    { "src": "/client/(.*)", "dest": "/client/$1" }
  ]
}